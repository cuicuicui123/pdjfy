<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/blue"
    android:elevation="@dimen/elevation_side_pop">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="姓名"
            android:textColor="@color/white"
            android:textSize="@dimen/text_size_user"
            android:layout_marginBottom="@dimen/margin_border"/>

        <include
            layout="@layout/divider"></include>

        <TextView
            android:id="@+id/tv_folder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="文件夹"
            android:textColor="@drawable/text_color_white_press_blue"
            android:textSize="@dimen/text_size_title"
            android:layout_marginTop="@dimen/margin_border"
            android:layout_marginBottom="@dimen/margin_border"/>

        <include
            layout="@layout/divider"></include>

        <TextView
            android:id="@+id/tv_return"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="回到首页"
            android:textColor="@drawable/text_color_white_press_blue"
            android:textSize="@dimen/text_size_title"
            android:layout_marginTop="@dimen/margin_border"/>

    </LinearLayout>


</RelativeLayout>